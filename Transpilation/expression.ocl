if condition then something else somethingElse endif

let something (one : onType, two : twoType) : someType = someValue in nestedExpression

not name0.base::leaf @ pre [qualifier1, qualifier2] (name0, name1 : typeBase::typeLeaf ; extraName : Integer = something | aParameter, anotherParameter)
*
multiple
+
sum
=
aValue[aqualifier, anotherQualifier]
and
(aBoolean or anotherOne)

Set{1..two, "three"..enum::literal}

path::name
